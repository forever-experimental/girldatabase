{
  "name": "girlsock.directory",
  "version": "0.9.0",
  "description": "",
  "scripts": {
    "build": "esbuild ./src/index.js --bundle --outfile=minified-bundle.js --minify",
    "build-debug": "esbuild ./src/index.js --bundle --outfile=minified-bundle.js --minify --sourcemap"
  },
  "keywords": [
    "cute"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-cognito-identity": "^3.511.0",
    "@aws-sdk/client-dynamodb": "^3.511.0",
    "@aws-sdk/credential-provider-cognito-identity": "^3.511.0",
    "@aws-sdk/util-dynamodb": "^3.511.0",
    "cute-socks": "^0.2.0",
    "cute-util": "^0.1.0",
    "lit-html": "^3.1.2"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "process": "^0.11.10"
  }
}
